version: '3'

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=find
      - POSTGRESQL_PASSWORD=find
      - POSTGRESQL_DATABASE=findAFriend